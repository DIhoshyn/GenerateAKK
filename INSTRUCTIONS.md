# 📖 Инструкция пользователя: MikroTik OVPN Deployer
Данное руководство описывает процесс подготовки данных, работы с базой паролей и автоматического развертывания учетных записей OpenVPN на маршрутизаторах MikroTik.

1. Подготовка Excel-файла
Для корректной работы программы ваш файл Excel должен иметь следующие колонки (заголовки):
* фамилия — фамилия пользователя (кириллица).
* имя — имя пользователя (кириллица).
* номер тф — контактный номер телефона.

Важно: программа автоматически выполнит транслитерацию ФИО. Мягкие и твердые знаки будут удалены для обеспечения совместимости с сетевыми стандартами.

2. Интеграция с KeePass
Программа поддерживает автоматическое извлечение паролей администратора MikroTik.
* В интерфейсе нажмите "Выбрать Базу .kdbx".
* Введите Мастер-пароль от вашей базы.
* Убедитесь, что в базе KeePass есть запись, где поле URL или Title совпадает с IP-адресом роутера, который вы ввели в поле "Host".

3. Процесс развертывания
* Запустите приложение и выберите файл Excel.
Выберите профиль OVPN в выпадающем списке:
* UFD — стандартный профиль.
* Cascad — для каскадных подключений.
Введите IP-адрес роутера (Host).
Нажмите "Запустить процесс".
Программа выполнит следующие действия:
Сгенерирует безопасные пароли (20 символов).
Создаст .rsc скрипт в папке files/.
Подключится по SSH, загрузит и выполнит скрипт на MikroTik.
Создаст отчет credentials.txt с данными для пользователей.

4. Возможные ошибки и их решение
* "Authentication failed": Проверьте правильность логина/пароля в KeePass или полях ввода. Убедитесь, что на MikroTik включен сервис SSH (/ip service print).
* "File not found": Проверьте, не открыт ли файл Excel в другой программе в момент запуска.
* "Syntax error в MikroTik": Убедитесь, что выбранный профиль (UFD или Cascad) заранее создан на роутере в меню /ppp profile.
