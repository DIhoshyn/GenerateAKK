MikroTik OVPN Deployer + KeePass Integration
MikroTik OVPN Deployer — это профессиональный инструмент для автоматизации создания VPN-аккаунтов на роутерах MikroTik. Приложение берет список пользователей из Excel, генерирует безопасные логины и пароли, транслитерирует данные для корректной работы в RouterOS и автоматически разворачивает конфигурацию на конечном оборудовании через SSH.

🚀 Основные возможности
Импорт из Excel: Автоматическая обработка списков (Фамилия, Имя, Телефон).

Умная транслитерация: Корректная обработка кириллицы (включая "ь", "ъ", "ы") для создания стабильных логинов в формате и.фамилия.

Безопасность прежде всего: Генерация паролей длиной 20 символов со спецсимволами и буквами разного регистра.

Интеграция с KeePass: Автоматическое получение учетных данных администратора MikroTik по IP-адресу или названию записи из вашей базы .kdbx.

Выбор профилей: Поддержка различных конфигураций OVPN (например, UFD или Cascad) прямо из интерфейса.

Автоматический деплой: Загрузка и выполнение .rsc скрипта на MikroTik через SSH в один клик.

Отчетность: Генерация человекочитаемого файла credentials.txt со всеми данными для передачи пользователям.

🛠 Технологический стек
Python 3.x

CustomTkinter — современный графический интерфейс.

Paramiko — работа с SSH/SFTP.

PyKeePass — интеграция с защищенными базами паролей.

Pandas — обработка табличных данных Excel.

📦 Установка и запуск
Склонируйте репозиторий:
git clone https://github.com/yourusername/mikrotik-ovpn-deployer.git
cd mikrotik-ovpn-deployer

Установите зависимости:
pip install -r requirements.txt

Запустите приложение:
python ui/main.py

Структура проекта
core/: Логика генерации учетных записей, SSH-клиент и работа с KeePass.

ui/: Графический интерфейс на CustomTkinter.

files/: Папка для хранения сгенерированных скриптов и отчетов.

config.json: Сохранение ваших настроек (IP хоста, логин, пути к базам).

⚠️ Требования к MikroTik
Включен сервис SSH (/ip service enable ssh).

Наличие прав write и test у пользователя.

Предварительно созданные профили PPP (UFD, Cascad).
